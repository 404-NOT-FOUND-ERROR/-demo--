# 懂了鸭 DEMO 交付规范

## 目录与命名

- 一个 Demo 一个独立目录：`demos/<demo-slug>/`。
- 目录名使用简短的小写英文与连字符，避免空格。
- `index.html` 是统一入口；独立资源统一放在 `assets/`。
- `preview.png` 使用 16:9 画面，展示核心体验而非编辑过程。

## 每个 Demo 的说明

项目 README 至少写清：

1. Demo 解决或讲解什么问题。
2. 用户从进入到完成的主要步骤。
3. 如何运行，以及是否依赖网络或外部服务。
4. 当前是概念演示、可交互原型还是可发布版本。
5. 已知限制和后续可改进项。

## 发布前检查

- 页面标题与可见品牌统一使用“懂了鸭 DEMO”。
- 首屏能明确说明主题，并提供清晰的开始入口。
- 主流程、返回、重新开始和关键状态可正常工作。
- 桌面端与移动端无明显裁切、遮挡和横向滚动。
- 外部资源加载失败时，不影响用户理解页面用途。
- 控制台无阻断主流程的错误。
- 入口文件、说明文档和预览图已经更新。

## 仓库维护

- `main` 始终保留可演示版本。
- 每次提交只聚焦一个 Demo 或一类维护工作。
- 提交信息建议使用：`feat: add <demo>`、`fix: improve <demo>`、`docs: update catalog`。
- 新 Demo 上线后同步更新根目录 README 的 Demo 目录。
